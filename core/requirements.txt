# SkillBridge Core — Python deps (Mục 2 của spec)
fastapi
uvicorn[standard]
python-multipart
python-dotenv
anthropic                 # provider mặc định (Claude) — nạp lười, chỉ cần khi MOCK=0
openai                    # khi LLM_PROVIDER=openai / endpoint tương thích OpenAI — nạp lười
sentence-transformers
numpy
pdfplumber
python-docx
